import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 20,
    backgroundColor: '#ed4d17ff',
    borderRadius: 8,
    alignItems: 'center',
  },
  titleStyle: {
    fontSize: 16,
    marginLeft: 10,
    color: '#fff',
  },
});
