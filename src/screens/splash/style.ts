import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  logo: {
    width: 100,
    height: 100,
    resizeMode: 'contain',
  },
  textStyle: {
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 20,
  },
  indicator: {
    marginTop: 20,
  },
});
